import { component } from "./elco";
import { computed, effect, reactive, watch } from "./reactivity";

const V = {
    reactive,
    effect,
    computed,
    watch,

    component
}

export default V
